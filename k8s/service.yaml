apiVersion: v1
kind: Service
metadata:
  name: github-files-ms
  labels:
    app: github-files-ms
spec:
  type: ClusterIP
  selector:
    app: github-files-ms
  ports:
    - name: http
      port: 80
      targetPort: 8080
