apiVersion: v1
kind: ConfigMap
metadata:
  name: github-files-ms-config
  labels:
    app: github-files-ms
data:
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "github_files_db"
  JWT_ISSUER_URI: "https://auth.example.com/realms/github-files"
  SERVER_PORT: "8080"
